{
  "name": "interactive-rating",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "tsc && eslint --fix",
    "clean": "rm -rf node_modules dist .turbo playwright-report",
    "test": "start-server-and-test dev http://localhost:5173 'playwright test'"
  },
  "devDependencies": {
    "@playwright/test": "^1.28.1",
    "eslint-config-custom": "workspace:*",
    "start-server-and-test": "^1.15.1",
    "typescript": "^4.6.4",
    "vite": "^3.2.3"
  }
}
